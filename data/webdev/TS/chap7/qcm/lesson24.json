[
  {
    "questionText": "Que fait le type utilitaire &lt;code&gt;Partial&lt;T&gt;&lt;/code&gt; en TypeScript ?",
    "options": [
      "Il rend toutes les propriétés de &lt;code&gt;T&lt;/code&gt; obligatoires.",
      "Il rend toutes les propriétés de &lt;code&gt;T&lt;/code&gt; optionnelles.",
      "Il supprime toutes les propriétés de &lt;code&gt;T&lt;/code&gt;.",
      "Il crée un nouveau type avec uniquement les propriétés optionnelles d'origine."
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`Partial&lt;T&gt;` est un type utilitaire qui transforme toutes les propriétés du type `T` en propriétés optionnelles. Cela est très utile pour les mises à jour partielles d'objets ou les paramètres de fonction où seules quelques propriétés sont fournies."
  },
  {
    "questionText": "Soit l'interface &lt;pre class=\"inline-code\"&gt;&lt;code&gt;interface User {\n  id: number;\n  name: string;\n  email?: string;\n}&lt;/code&gt;&lt;/pre&gt; Que devient le type &lt;code&gt;Required&lt;User&gt;&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;{ id: number; name: string; email?: string }&lt;/code&gt; (inchangé)",
      "&lt;code&gt;{ id: number; name: string; email: string }&lt;/code&gt; (email devient obligatoire)",
      "&lt;code&gt;{ id?: number; name?: string; email?: string }&lt;/code&gt;",
      "Une erreur de compilation car &lt;code&gt;email&lt;/code&gt; est déjà optionnel"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`Required&lt;T&gt;` rend toutes les propriétés de `T` obligatoires, y compris celles qui étaient optionnelles. Ici, `email?: string` devient `email: string`. Cela force la présence de toutes les propriétés lorsqu'on utilise ce type."
  },
  {
    "questionText": "Quelle est l'utilité principale du type &lt;code&gt;Readonly&lt;T&gt;&lt;/code&gt; ?",
    "options": [
      "Permettre la modification des propriétés d'un objet.",
      "Empêcher la modification des propriétés d'un objet (les rendre immuables).",
      "Supprimer les propriétés en lecture seule.",
      "Rendre les propriétés optionnelles."
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`Readonly&lt;T&gt;` transforme toutes les propriétés de `T` en propriétés en lecture seule (`readonly`). Toute tentative de modification après la création générera une erreur TypeScript. C'est utile pour garantir l'immutabilité des données."
  },
  {
    "questionText": "Avec &lt;code&gt;Pick&lt;T, K&gt;&lt;/code&gt;, que sélectionne-t-on ?",
    "options": [
      "Toutes les propriétés de &lt;code&gt;T&lt;/code&gt; sauf celles dans &lt;code&gt;K&lt;/code&gt;.",
      "Un sous-ensemble des propriétés de &lt;code&gt;T&lt;/code&gt; dont les noms sont dans l'union &lt;code&gt;K&lt;/code&gt;.",
      "Les propriétés de &lt;code&gt;T&lt;/code&gt; qui sont optionnelles.",
      "Les méthodes de &lt;code&gt;T&lt;/code&gt; uniquement."
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`Pick&lt;T, K&gt;` construit un nouveau type en sélectionnant uniquement les propriétés de `T` dont les clés sont dans l'union `K`. C'est utile pour créer des types plus petits à partir d'un type existant. Exemple : `Pick&lt;User, 'id' | 'name'&gt;` donne un type avec uniquement `id` et `name`."
  },
  {
    "questionText": "Que fait &lt;code&gt;Omit&lt;T, K&gt;&lt;/code&gt; ?",
    "options": [
      "Il sélectionne uniquement les propriétés dont les clés sont dans &lt;code&gt;K&lt;/code&gt;.",
      "Il supprime les propriétés de &lt;code&gt;T&lt;/code&gt; dont les clés sont dans l'union &lt;code&gt;K&lt;/code&gt;.",
      "Il rend toutes les propriétés optionnelles.",
      "Il ajoute des propriétés à &lt;code&gt;T&lt;/code&gt;."
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`Omit&lt;T, K&gt;` est l'inverse de `Pick`. Il construit un nouveau type en supprimant les propriétés de `T` dont les clés sont dans l'union `K`. Exemple : `Omit&lt;User, 'email'&gt;` donne un type `User` sans la propriété `email`."
  },
  {
    "questionText": "Soit &lt;code&gt;type T = string | number | boolean | null | undefined&lt;/code&gt;. Que produit &lt;code&gt;Exclude&lt;T, null | undefined&gt;&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;string | number | boolean&lt;/code&gt;",
      "&lt;code&gt;null | undefined&lt;/code&gt;",
      "&lt;code&gt;string | number | boolean | null | undefined&lt;/code&gt; (inchangé)",
      "&lt;code&gt;never&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`Exclude&lt;T, U&gt;` supprime de l'union `T` tous les types qui sont assignables à `U`. Ici, `null` et `undefined` sont retirés, il reste donc `string | number | boolean`. C'est très utile pour filtrer des unions de types."
  },
  {
    "questionText": "Quelle est la différence entre &lt;code&gt;Exclude&lt;/code&gt; et &lt;code&gt;Extract&lt;/code&gt; ?",
    "options": [
      "`Exclude` garde les types communs, `Extract` supprime les types.",
      "`Exclude` supprime les types assignables à U, `Extract` garde uniquement les types assignables à U.",
      "`Exclude` fonctionne uniquement sur les objets, `Extract` sur les unions.",
      "Il n'y a pas de différence, ce sont des alias."
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "`Exclude&lt;T, U&gt;` supprime de `T` les types qui sont dans `U` (ou assignables à `U`). `Extract&lt;T, U&gt;` fait l'inverse : il garde uniquement les types de `T` qui sont dans `U` (ou assignables à `U`). Exemple : `Extract&lt;string | number, string&gt;` donne `string`."
  },
  {
    "questionText": "Que retourne &lt;code&gt;NonNullable&lt;string | null | undefined&gt;&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;string | null | undefined&lt;/code&gt;",
      "&lt;code&gt;string&lt;/code&gt;",
      "&lt;code&gt;never&lt;/code&gt;",
      "&lt;code&gt;string | null&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`NonNullable&lt;T&gt;` supprime `null` et `undefined` de l'union `T`. Ici, `string | null | undefined` devient simplement `string`. C'est pratique pour assurer qu'une valeur ne peut être ni `null` ni `undefined`."
  },
  {
    "questionText": "Observez ce code : &lt;pre class=\"inline-code\"&gt;&lt;code&gt;interface Config {\n  url: string;\n  timeout: number;\n  retries?: number;\n}\ntype PartialConfig = Partial&lt;Config&gt;;&lt;/code&gt;&lt;/pre&gt; Que peut-on faire avec &lt;code&gt;PartialConfig&lt;/code&gt; ?",
    "options": [
      "Créer un objet qui doit avoir absolument toutes les propriétés de &lt;code&gt;Config&lt;/code&gt;.",
      "Créer un objet où &lt;code&gt;url&lt;/code&gt;, &lt;code&gt;timeout&lt;/code&gt; et &lt;code&gt;retries&lt;/code&gt; sont tous optionnels.",
      "Créer un objet sans aucune propriété.",
      "Créer un objet où &lt;code&gt;retries&lt;/code&gt; devient obligatoire."
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`Partial&lt;Config&gt;` rend chaque propriété de `Config` optionnelle. Ainsi, `PartialConfig` équivaut à `{ url?: string; timeout?: number; retries?: number; }`. Cela permet de créer des objets de configuration partielle, par exemple pour des mises à jour."
  },
  {
    "questionText": "Quel type utilitaire utiliserait-on pour créer un type &lt;code&gt;UserPreview&lt;/code&gt; contenant uniquement &lt;code&gt;id&lt;/code&gt; et &lt;code&gt;name&lt;/code&gt; à partir de &lt;code&gt;User&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;Omit&lt;User, 'id' | 'name'&gt;&lt;/code&gt;",
      "&lt;code&gt;Partial&lt;User&gt;&lt;/code&gt;",
      "&lt;code&gt;Pick&lt;User, 'id' | 'name'&gt;&lt;/code&gt;",
      "&lt;code&gt;Exclude&lt;User, 'email'&gt;&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "`Pick&lt;User, 'id' | 'name'&gt;` sélectionne uniquement les propriétés `id` et `name` de l'interface `User`. C'est parfait pour créer une vue allégée d'un utilisateur, sans exposer toutes ses données."
  },
  {
    "questionText": "Laquelle de ces affirmations sur &lt;code&gt;Readonly&lt;/code&gt; est &lt;strong&gt;fausse&lt;/strong&gt; ?",
    "options": [
      "Il peut être combiné avec d'autres types utilitaires comme &lt;code&gt;Partial&lt;/code&gt;.",
      "Les propriétés d'un objet &lt;code&gt;Readonly&lt;/code&gt; ne peuvent pas être modifiées après création (vérification à la compilation).",
      "Le type utilitaire &lt;code&gt;Readonly&lt;/code&gt; fonctionne également avec les tableaux pour empêcher les mutations.",
      "Le mot-clé &lt;code&gt;readonly&lt;/code&gt; natif et &lt;code&gt;Readonly&lt;T&gt;&lt;/code&gt; produisent des comportements différents à l'exécution."
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "`readonly` (propriété individuelle) et `Readonly&lt;T&gt;` (type utilitaire) agissent tous les deux uniquement au niveau de la compilation (TypeScript). À l'exécution, ce sont de simples objets JavaScript modifiables. La différence est donc uniquement syntaxique, pas comportementale à l'exécution."
  },
  {
    "questionText": "Soient les types : &lt;pre class=\"inline-code\"&gt;&lt;code&gt;type A = 'a' | 'b' | 'c';\ntype B = 'b' | 'c' | 'd';&lt;/code&gt;&lt;/pre&gt; Que donne &lt;code&gt;Exclude&lt;A, B&gt;&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;'a' | 'b' | 'c' | 'd'&lt;/code&gt;",
      "&lt;code&gt;'b' | 'c'&lt;/code&gt;",
      "&lt;code&gt;'a'&lt;/code&gt;",
      "&lt;code&gt;'d'&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "`Exclude&lt;A, B&gt;` garde les éléments de `A` qui ne sont pas dans `B`. `'a'` est dans `A` mais pas dans `B`, donc le résultat est `'a'`. `'b'` et `'c'` sont supprimés car présents dans `B`."
  }
]