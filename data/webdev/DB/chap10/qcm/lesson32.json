[
  {
    "questionText": "Quelle est la principale différence entre une sauvegarde logique et une sauvegarde physique ?",
    "options": [
      "La sauvegarde logique est plus rapide que la sauvegarde physique",
      "La sauvegarde logique sauvegarde les données sous forme de requêtes SQL (&lt;code&gt;INSERT&lt;/code&gt;, &lt;code&gt;CREATE TABLE&lt;/code&gt;) alors que la sauvegarde physique copie les fichiers bruts de la base de données",
      "La sauvegarde physique ne peut être restaurée que sur le même serveur, contrairement à la sauvegarde logique",
      "Il n’y a pas de différence, ce sont deux noms pour la même chose"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Une sauvegarde logique (ex: &lt;code&gt;mysqldump&lt;/code&gt;, &lt;code&gt;pg_dump&lt;/code&gt;) produit un fichier avec les instructions SQL nécessaires pour reconstruire la base (schéma + données). Une sauvegarde physique copie directement les fichiers de données (fichiers &lt;code&gt;.ibd&lt;/code&gt;, &lt;code&gt;.frm&lt;/code&gt; ou répertoires PostgreSQL) depuis le système de fichiers. La rapidité dépend du contexte, et une sauvegarde physique peut souvent être déplacée vers un autre serveur si le système est compatible."
  },
  {
    "questionText": "En utilisant &lt;code&gt;mysqldump&lt;/code&gt;, quelle commande permet de sauvegarder uniquement la structure (sans les données) de la base &lt;code&gt;ecole&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;mysqldump -u root -p ecole &gt; backup.sql&lt;/code&gt;",
      "&lt;code&gt;mysqldump -u root -p --no-data ecole &gt; structure.sql&lt;/code&gt;",
      "&lt;code&gt;mysqldump -u root -p --no-create-info ecole &gt; data.sql&lt;/code&gt;",
      "&lt;code&gt;mysqldump -u root -p --all-databases &gt; tout.sql&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "L'option &lt;code&gt;--no-data&lt;/code&gt; indique à &lt;code&gt;mysqldump&lt;/code&gt; de ne pas inclure les lignes des tables (seulement le &lt;code&gt;CREATE TABLE&lt;/code&gt; et autres objets). &lt;code&gt;--no-create-info&lt;/code&gt; fait l'inverse (seulement les données sans la structure). La première commande sauvegarde tout (structure + données). La dernière sauvegarde tous les serveurs."
  },
  {
    "questionText": "Sous Linux, quel outil ou service permet d’automatiser une tâche comme &lt;code&gt;mysqldump&lt;/code&gt; chaque nuit à 2h00 du matin ?",
    "options": [
      "Systemd",
      "Anacron",
      "Cron",
      "At"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Cron est le planificateur de tâches standard sous Linux/Unix. On peut ajouter une ligne dans la crontab (&lt;code&gt;crontab -e&lt;/code&gt;) du type &lt;code&gt;0 2 * * * /usr/bin/mysqldump ...&lt;/code&gt;. Systemd peut aussi planifier via des timers, mais c'est plus complexe. Anacron est pour les systèmes non continuellement allumés. At est pour une exécution unique différée."
  },
  {
    "questionText": "Pour restaurer une base PostgreSQL à partir d’une sauvegarde logique au format personnalisé (&lt;code&gt;.dump&lt;/code&gt;), quelle commande est appropriée ?",
    "options": [
      "&lt;code&gt;psql -d mabase &lt; fichier.dump&lt;/code&gt;",
      "&lt;code&gt;pg_restore -d mabase fichier.dump&lt;/code&gt;",
      "&lt;code&gt;pg_dump -d mabase fichier.dump&lt;/code&gt;",
      "&lt;code&gt;restore postgres -i fichier.dump&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;pg_restore&lt;/code&gt; est l'outil conçu pour restaurer les sauvegardes au format non-plain (personnalisé, tar ou répertoire) produites par &lt;code&gt;pg_dump -Fc&lt;/code&gt;. &lt;code&gt;psql&lt;/code&gt; ne lit que les fichiers texte SQL classiques. &lt;code&gt;pg_dump&lt;/code&gt; sert à exporter, pas à restaurer."
  },
  {
    "questionText": "Quel est l’inconvénient majeur d’une sauvegarde physique par rapport à une sauvegarde logique ?",
    "options": [
      "Elle est impossible à compresser",
      "Elle est souvent moins portable (peut dépendre de la version du SGBD, du système d’exploitation ou de l’architecture matérielle)",
      "Elle ne sauvegarde pas les index",
      "Elle ne peut pas être utilisée pour une réplication"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Les fichiers physiques (ex: fichiers &lt;code&gt;.ibd&lt;/code&gt; de MySQL/InnoDB, répertoire &lt;code&gt;base/&lt;/code&gt; de PostgreSQL) sont très dépendants de la version exacte du SGBD, de l'endianness du processeur, et parfois du système d'exploitation. Une sauvegarde logique, étant du texte SQL, est beaucoup plus portable entre versions et plateformes. En revanche, une sauvegarde physique se restaure généralement plus vite."
  },
  {
    "questionText": "Quelle commande &lt;code&gt;mysqldump&lt;/code&gt; permet de sauvegarder plusieurs bases spécifiques (&lt;code&gt;clients&lt;/code&gt; et &lt;code&gt;commandes&lt;/code&gt;) en un seul fichier ?",
    "options": [
      "&lt;code&gt;mysqldump -u root -p clients commandes &gt; backup.sql&lt;/code&gt;",
      "&lt;code&gt;mysqldump -u root -p --databases clients commandes &gt; backup.sql&lt;/code&gt;",
      "&lt;code&gt;mysqldump -u root -p --all-databases --include=clients,commandes &gt; backup.sql&lt;/code&gt;",
      "&lt;code&gt;mysqldump -u root -p --tables clients commandes &gt; backup.sql&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "L'option &lt;code&gt;--databases&lt;/code&gt; (ou &lt;code&gt;-B&lt;/code&gt;) permet de lister plusieurs bases à sauvegarder. Sans cette option, &lt;code&gt;mysqldump&lt;/code&gt; considère le premier argument comme le nom de la base et le second éventuellement comme une table. La bonne syntaxe est donc &lt;code&gt;mysqldump ... --databases clients commandes &gt; backup.sql&lt;/code&gt;."
  },
  {
    "questionText": "Lors d’une sauvegarde logique avec &lt;code&gt;pg_dump&lt;/code&gt;, que signifie l’option &lt;code&gt;-Fc&lt;/code&gt; ?",
    "options": [
      "Format compressé (avec gzip)",
      "Format personnalisé (custom), compressé et permettant une restauration parallèle avec pg_restore",
      "Format CSV (Comma Separated Values)",
      "Forcer la compression au niveau maximum"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "&lt;code&gt;-Fc&lt;/code&gt; (ou &lt;code&gt;--format=custom&lt;/code&gt;) produit un fichier au format propriétaire de PostgreSQL. Ce format est compressé par défaut et permet d'utiliser &lt;code&gt;pg_restore&lt;/code&gt; avec l'option &lt;code&gt;-j&lt;/code&gt; pour une restauration parallèle (plusieurs jobs). Ce n'est pas du simple gzip, c'est un format structuré."
  },
  {
    "questionText": "Parmi les propositions suivantes, laquelle est une bonne pratique pour automatiser les sauvegardes via cron ?",
    "options": [
      "Écrire le mot de passe en clair dans le script bash avec &lt;code&gt;-pmonMotDePasse&lt;/code&gt;",
      "Utiliser un fichier de configuration &lt;code&gt;.my.cnf&lt;/code&gt; (pour MySQL) ou &lt;code&gt;.pgpass&lt;/code&gt; (pour PostgreSQL) avec les droits restreints (&lt;code&gt;chmod 600&lt;/code&gt;)",
      "Désactiver la journalisation des sauvegardes",
      "Sauvegarder directement dans &lt;code&gt;/tmp&lt;/code&gt; pour éviter les problèmes de droits"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Mettre le mot de passe en clair dans un script est dangereux. Les fichiers &lt;code&gt;.my.cnf&lt;/code&gt; (MySQL) ou &lt;code&gt;.pgpass&lt;/code&gt; (PostgreSQL) permettent de stocker les identifiants de manière plus sécurisée, à condition de restreindre leurs droits en lecture (&lt;code&gt;chmod 600&lt;/code&gt;). Sauvegarder dans &lt;code&gt;/tmp&lt;/code&gt; n'est pas fiable (peut être effacé)."
  }
]