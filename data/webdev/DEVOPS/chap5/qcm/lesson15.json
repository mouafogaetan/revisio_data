[
  {
    "questionText": "Quelle est la principale différence technique entre un conteneur Docker et une machine virtuelle traditionnelle ?",
    "options": [
      "Les conteneurs virtualisent le noyau de l'OS alors que les machines virtuelles virtualisent le matériel.",
      "Les conteneurs incluent toujours un système d'exploitation complet alors que les machines virtuelles ne le font pas.",
      "Les machines virtuelles démarent plus rapidement que les conteneurs.",
      "Les conteneurs nécessitent un hyperviseur pour fonctionner, contrairement aux machines virtuelles."
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "La réponse correcte est : 'Les conteneurs virtualisent le noyau de l'OS alors que les machines virtuelles virtualisent le matériel.' Les conteneurs partagent le noyau de l'hôte (via des espaces de noms et cgroups), ce qui les rend légers et rapides. Les machines virtuelles émulent du matériel complet et nécessitent un hyperviseur, avec son propre OS invité."
  },
  {
    "questionText": "Parmi ces commandes, laquelle permet d'installer Docker sur une distribution Linux basée sur Debian/Ubuntu selon la documentation officielle ?",
    "options": [
      "&lt;code&gt;sudo snap install docker&lt;/code&gt;",
      "&lt;code&gt;sudo apt install docker.io&lt;/code&gt;",
      "&lt;code&gt;sudo apt update && sudo apt install docker-ce docker-ce-cli containerd.io&lt;/code&gt;",
      "&lt;code&gt;sudo yum install docker&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "La réponse correcte utilise le dépôt officiel de Docker (docker-ce). L'option B (&lt;code&gt;docker.io&lt;/code&gt;) est le paquet de dépôt Ubuntu/Debian, souvent plus ancien. L'option A (snap) est alternative mais pas la principale pour Debian/Ubuntu. L'option D (&lt;code&gt;yum&lt;/code&gt;) est pour Red Hat/CentOS. La méthode officielle recommandée est d'ajouter le dépôt Docker puis d'installer 'docker-ce'."
  },
  {
    "questionText": "Dans l'architecture Docker, quel composant est responsable de la construction, de l'exécution et de la distribution des conteneurs, et s'exécute en tant que service d'arrière-plan (daemon) ?",
    "options": [
      "Docker Client (&lt;code&gt;docker&lt;/code&gt;)",
      "Docker Daemon (&lt;code&gt;dockerd&lt;/code&gt;)",
      "Docker Registry (comme Docker Hub)",
      "Docker Compose"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Le Docker Daemon (&lt;code&gt;dockerd&lt;/code&gt;) est le service qui gère les images, conteneurs, réseaux et volumes. Le client (&lt;code&gt;docker&lt;/code&gt;) communique avec le daemon via une API. Le registry stocke les images, et Docker Compose est un outil pour définir des applications multi-conteneurs."
  },
  {
    "questionText": "Quel registre d'images Docker est public par défaut et héberge des milliers d'images officielles comme &lt;code&gt;ubuntu&lt;/code&gt;, &lt;code&gt;nginx&lt;/code&gt; ou &lt;code&gt;node&lt;/code&gt; ?",
    "options": [
      "Google Container Registry (GCR)",
      "Amazon Elastic Container Registry (ECR)",
      "Docker Hub",
      "GitHub Container Registry (GHCR)"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Docker Hub est le registre public par défaut configuré dans Docker. C'est le plus grand catalogue d'images pré-construites. Les autres (GCR, ECR, GHCR) sont des registres cloud mais ne sont pas le registre par défaut de Docker."
  },
  {
    "questionText": "Que fait la commande &lt;code&gt;docker run -it ubuntu bash&lt;/code&gt; ?",
    "options": [
      "Elle télécharge l'image ubuntu sans l'exécuter.",
      "Elle crée et exécute un conteneur Ubuntu en mode interactif avec un shell bash.",
      "Elle liste tous les conteneurs Ubuntu en cours d'exécution.",
      "Elle supprime tous les conteneurs arrêtés basés sur Ubuntu."
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "L'option &lt;code&gt;-it&lt;/code&gt; combine &lt;code&gt;-i&lt;/code&gt; (interactif) et &lt;code&gt;-t&lt;/code&gt; (pseudo-TTY). 'ubuntu' est le nom de l'image, et 'bash' est la commande à exécuter dans le conteneur. Cela permet d'avoir un terminal interactif dans un conteneur Ubuntu."
  },
  {
    "questionText": "Pour visualiser les logs d'un conteneur déjà démarré nommé 'webserver', vous utilisez :",
    "options": [
      "&lt;code&gt;docker ps webserver&lt;/code&gt;",
      "&lt;code&gt;docker inspect webserver&lt;/code&gt;",
      "&lt;code&gt;docker logs webserver&lt;/code&gt;",
      "&lt;code&gt;docker exec webserver logs&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La commande &lt;code&gt;docker logs&lt;/code&gt; affiche les logs (stdout/stderr) d'un conteneur. &lt;code&gt;docker ps&lt;/code&gt; liste les conteneurs, &lt;code&gt;docker inspect&lt;/code&gt; donne des détails de configuration, et &lt;code&gt;docker exec&lt;/code&gt; exécute une commande à l'intérieur."
  },
  {
    "questionText": "Sous Linux, quel argument doit-on ajouter à &lt;code&gt;docker run&lt;/code&gt; pour rendre accessible un port du conteneur (80) sur un port de l'hôte (8080) ?",
    "options": [
      "&lt;code&gt;-p 80:8080&lt;/code&gt;",
      "&lt;code&gt;-p 8080:80&lt;/code&gt;",
      "&lt;code&gt;--expose 80&lt;/code&gt;",
      "&lt;code&gt;--network host&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "La syntaxe est &lt;code&gt;-p PORT_HOTE:PORT_CONTENEUR&lt;/code&gt;. Donc &lt;code&gt;-p 8080:80&lt;/code&gt; expose le port 80 du conteneur sur le port 8080 de l'hôte. &lt;code&gt;--expose&lt;/code&gt; est documentaire, et &lt;code&gt;--network host&lt;/code&gt; supprime l'isolation réseau."
  },
  {
    "questionText": "Quelle commande permet d'exécuter une commande supplémentaire (&lt;code&gt;ls -l /app&lt;/code&gt;) dans un conteneur déjà en cours d'exécution nommé 'mon_app' ?",
    "options": [
      "&lt;code&gt;docker attach mon_app -- ls -l /app&lt;/code&gt;",
      "&lt;code&gt;docker exec mon_app ls -l /app&lt;/code&gt;",
      "&lt;code&gt;docker run mon_app ls -l /app&lt;/code&gt;",
      "&lt;code&gt;docker shell mon_app ls -l /app&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;docker exec&lt;/code&gt; est la commande à utiliser pour exécuter un processus dans un conteneur existant. &lt;code&gt;docker attach&lt;/code&gt; se connecte au processus principal, &lt;code&gt;docker run&lt;/code&gt; crée un nouveau conteneur, et &lt;code&gt;docker shell&lt;/code&gt; n'existe pas."
  },
  {
    "questionText": "Pour garantir qu'un fichier généré par un conteneur (par exemple une base de données SQLite) persiste après la suppression de ce conteneur, quelle est la meilleure solution ?",
    "options": [
      "Copier le fichier manuellement avec &lt;code&gt;docker cp&lt;/code&gt; avant d'arrêter le conteneur.",
      "Utiliser un volume Docker (&lt;code&gt;-v mon_volume:/chemin/dans/conteneur&lt;/code&gt;).",
      "Commit le conteneur en image avec &lt;code&gt;docker commit&lt;/code&gt;.",
      "Utiliser le répertoire par défaut &lt;code&gt;/var/lib/docker&lt;/code&gt; directement."
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Les volumes Docker sont la méthode officielle pour persister des données indépendamment du cycle de vie du conteneur. &lt;code&gt;docker cp&lt;/code&gt; est ponctuel. &lt;code&gt;docker commit&lt;/code&gt; crée une image figée et n'est pas idéal pour des données volatiles. Accéder directement à &lt;code&gt;/var/lib/docker&lt;/code&gt; est déconseillé et dangereux."
  },
  {
    "questionText": "Quelle commande permet de supprimer un conteneur qui est actuellement arrêté ?",
    "options": [
      "&lt;code&gt;docker delete mon_conteneur&lt;/code&gt;",
      "&lt;code&gt;docker rm mon_conteneur&lt;/code&gt;",
      "&lt;code&gt;docker rmi mon_conteneur&lt;/code&gt;",
      "&lt;code&gt;docker stop mon_conteneur&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "&lt;code&gt;docker rm&lt;/code&gt; supprime un conteneur arrêté. &lt;code&gt;docker rmi&lt;/code&gt; supprime une image. &lt;code&gt;docker stop&lt;/code&gt; arrête un conteneur mais ne le supprime pas. &lt;code&gt;docker delete&lt;/code&gt; n'existe pas."
  }
]