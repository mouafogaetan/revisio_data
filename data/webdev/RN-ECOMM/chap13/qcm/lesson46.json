[
  {
    "questionText": "Qu'est-ce que Detox dans l'écosystème React Native ?",
    "options": [
      "Un outil de debugging pour analyser les performances",
      "Une bibliothèque de tests unitaires pour les composants React",
      "Un framework de tests end-to-end (E2E) pour les applications React Native",
      "Un outil de打包 (bundling) alternatif à Metro"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Detox est un framework de tests end-to-end (E2E) spécifiquement conçu pour les applications React Native. Il permet de tester le flux complet de l'application comme un utilisateur réel : lancer l'app, interagir avec l'interface, vérifier les résultats. Contrairement à Jest (tests unitaires) ou React Native Testing Library (tests d'intégration), Detox exécute les tests sur un vrai appareil ou simulateur."
  },
  {
    "questionText": "Quelle commande npm installe correctement Detox dans un projet React Native ?",
    "options": [
      "`npm install detox --save-dev`",
      "`npm install @testing-library/react-native`",
      "`npm install jest detox --save-dev`",
      "`npm install detox jest --global`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "La commande correcte est `npm install detox --save-dev` (ou `yarn add detox --dev`). Detox doit être installé comme dépendance de développement. Il faut également installer `jest` si ce n'est pas déjà fait, car Detox utilise Jest comme test runner. L'option `--global` n'est pas recommandée pour les dépendances de projet."
  },
  {
    "questionText": "Voici un extrait de configuration Detox. Quel est le rôle de la propriété `binaryPath` ?\n```json\n{\n  \"apps\": {\n    \"ios.debug\": {\n      \"type\": \"ios.app\",\n      \"binaryPath\": \"ios/build/Build/Products/Debug-iphonesimulator/MyApp.app\",\n      \"build\": \"xcodebuild -workspace ios/MyApp.xcworkspace -scheme MyApp -configuration Debug -sdk iphonesimulator -derivedDataPath ios/build\"\n    }\n  }\n}\n```",
    "options": [
      "Le chemin vers le code source de l'application",
      "Le chemin vers le fichier `.app` compilé à installer sur le simulateur",
      "Le chemin vers les rapports de test",
      "Le chemin vers les logs de l'application"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "La propriété `binaryPath` indique à Detox l'emplacement du fichier `.app` (iOS) ou `.apk`/`.aab` (Android) qui doit être installé et exécuté sur le simulateur/device. Ce fichier est le résultat de la compilation (build) de l'application. La propriété `build` contient la commande pour générer ce binaire."
  },
  {
    "questionText": "Dans un test Detox, comment simuler la saisie de texte dans un champ email ?",
    "options": [
      "`await element(by.id('emailInput')).type('user@example.com');`",
      "`await expect(element(by.id('emailInput'))).toHaveText('user@example.com');`",
      "`await element(by.id('emailInput')).tap(); await element(by.id('emailInput')).text('user@example.com');`",
      "`await element(by.id('emailInput')).replaceText('user@example.com');`"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "Pour saisir du texte dans un champ avec Detox, on utilise la méthode `.replaceText()` qui efface le contenu existant et tape le nouveau texte. La méthode `.type()` existe aussi mais tape caractère par caractère (plus lent, plus réaliste). Cependant, la réponse `replaceText` est plus fréquemment utilisée car plus rapide et fiable. La bonne syntaxe est `await element(by.id('emailInput')).replaceText('user@example.com');`."
  },
  {
    "questionText": "Comment Detox identifie-t-il un élément sur l'écran ?",
    "options": [
      "Uniquement par le texte affiché",
      "Uniquement par le type de composant (Button, Text, etc.)",
      "Via des identifiants de test (testID) ou d'autres attributs comme le texte, l'accessibilité, etc.",
      "Via des sélecteurs CSS comme sur le web"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Detox utilise une API de sélection (matchers) pour identifier les éléments. Les méthodes principales sont : `by.id('testID')` (recommandé), `by.text('texte')`, `by.type('RCTTextField')`, `by.label('label')`, etc. La meilleure pratique est d'ajouter `testID` aux composants importants dans le code React Native, ce qui rend les tests plus robustes et moins sensibles aux changements d'interface."
  },
  {
    "questionText": "Quelle est la différence fondamentale entre un test Detox et un test avec React Native Testing Library ?",
    "options": [
      "Detox teste le comportement sur un vrai appareil/simulateur (E2E), tandis que Testing Library teste le rendu des composants en mémoire (tests d'intégration)",
      "Il n'y a pas de différence, ils font la même chose",
      "Detox est plus rapide que Testing Library",
      "Testing Library peut tester des gestes multi-touch, pas Detox"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "La différence est fondamentale : Detox exécute les tests end-to-end sur un émulateur ou un vrai appareil, en interagissant réellement avec l'application (comme un utilisateur). React Native Testing Library (souvent avec Jest) teste les composants React en mémoire, sans émuler l'environnement iOS/Android complet. Detox est plus lent mais teste l'intégration complète (API, navigation, stockage, etc.), tandis que Testing Library est plus rapide pour tester la logique et le rendu des composants."
  },
  {
    "questionText": "Voici un snippet de test Detox. Quelle ligne contient une erreur de syntaxe ?\n```javascript\ndescribe('Login flow', () =&gt; {\n  beforeEach(async () =&gt; {\n    await device.reloadReactNative();\n  });\n\n  it('should login successfully', async () =&gt; {\n    await element(by.id('email')).replaceText('user@example.com');  // Ligne A\n    await element(by.id('password')).replaceText('password123');     // Ligne B\n    await element(by.id('loginButton')).tap();                      // Ligne C\n    await expect(element(by.text('Bienvenue'))).toBeExist();        // Ligne D\n  });\n});\n```",
    "options": [
      "Ligne A : `replaceText` devrait être `typeText`",
      "Ligne B : le mot de passe ne peut pas être saisi en clair",
      "Ligne C : `tap()` doit être suivi d'un `await` supplémentaire",
      "Ligne D : `toBeExist()` devrait être `toBeVisible()` ou `toExist()`"
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "La méthode correcte est `toExist()` ou `toBeVisible()` selon la version de Detox. `toBeExist()` n'existe pas dans l'API standard de Detox. Les matchers d'attente sont : `toBeVisible()`, `toBeNotVisible()`, `toExist()`, `toNotExist()`, `toHaveText()`, `toHaveId()`, etc. L'erreur serait levée à l'exécution car `toBeExist` n'est pas une fonction reconnue."
  },
  {
    "questionText": "Pour exécuter des tests Detox sur Android, quelle configuration supplémentaire est généralement nécessaire ?",
    "options": [
      "Aucune, Detox fonctionne parfaitement sans configuration Android",
      "Installer et configurer un émulateur AVD (Android Virtual Device) avec les paramètres appropriés",
      "Compiler l'application avec Gradle en mode release uniquement",
      "Désinstaller complètement Expo"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Pour exécuter des tests Detox sur Android, il faut : 1) un émulateur AVD configuré (ou un vrai appareil avec débogage USB), 2) spécifier le `type: 'android.apk'` dans la configuration, 3) le `binaryPath` pointant vers le fichier .apk (généralement dans `android/app/build/outputs/apk/debug/`), 4) la commande `build` utilisant Gradle (`cd android && ./gradlew assembleDebug assembleAndroidTest`). Detox ne fonctionne pas 'out of the box' sans ces étapes."
  },
  {
    "questionText": "Quelle méthode Detox permet d'attendre qu'un élément soit visible avant d'interagir avec lui (ex: après une navigation) ?",
    "options": [
      "`await waitFor(element(by.id('screen'))).toBeVisible().withTimeout(5000);`",
      "`await expect(element(by.id('screen'))).toBeVisible(5000);`",
      "`await element(by.id('screen')).waitToBeVisible(5000);`",
      "`await delay(5000);`"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "Detox utilise l'API `waitFor` pour synchroniser avec les actions asynchrones. La syntaxe correcte est : `await waitFor(element(by.id('screen'))).toBeVisible().withTimeout(5000);`. Cela permet d'attendre jusqu'à 5 secondes que l'élément devienne visible avant de poursuivre. Utiliser un simple `delay` n'est pas fiable et ralentit les tests."
  },
  {
    "questionText": "Que signifie la commande `detox test --configuration ios.sim.release` ?",
    "options": [
      "Elle exécute les tests sur un vrai iPhone en mode release",
      "Elle exécute les tests sur le simulateur iOS avec la configuration 'release' (généralement binaire optimisé)",
      "Elle génère un rapport de test au format XML",
      "Elle nettoie les caches de build avant d'exécuter les tests"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "La commande `detox test --configuration ios.sim.release` lance les tests Detox en utilisant la configuration nommée `ios.sim.release` définie dans `package.json` ou `.detoxrc.json`. Cette configuration pointe généralement vers un binaire de l'application en mode 'release' (optimisé, sans outils de debug). Le flag `--configuration` permet de choisir la plateforme (iOS/Android), le type d'appareil (simulateur/device) et le mode de build (debug/release)."
  },
  {
    "questionText": "Comment Detox gère-t-il la synchronisation automatique avec l'application ?",
    "options": [
      "Il utilise des `setTimeout` automatiques partout",
      "Il s'intègre au framework React Native pour attendre que le bridge JS soit inactif, les animations terminées et les requêtes réseau finies",
      "Il ne gère pas la synchronisation, le développeur doit ajouter des `await delay()` manuels",
      "Il utilise une IA pour prédire quand l'application est prête"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Detox dispose d'un mécanisme de synchronisation automatique qui surveille : 1) le bus React Native (attends que le bridge JavaScript soit idle), 2) les animations, 3) les requêtes réseau, 4) les timers, 5) les gestes. C'est ce qui distingue Detox d'autres frameworks E2E. On peut désactiver certaines synchronisations avec `device.disableSynchronization()` si nécessaire (ex: pour tester des animations longues)."
  },
  {
    "questionText": "Quelle est la meilleure pratique pour identifier un élément dans un test Detox ?",
    "options": [
      "Utiliser `by.text()` sur le contenu textuel exact",
      "Utiliser `by.type()` sur le type de composant (ex: 'RCTTextView')",
      "Utiliser un attribut `testID` dédié dans le code source, puis `by.id('monId')` dans le test",
      "Utiliser `by.traits()` pour les éléments accessibles"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La meilleure pratique est d'ajouter un `testID` unique à chaque élément interactif ou important dans le code React Native (`&lt;TouchableOpacity testID=\"loginButton\"&gt;...&lt;/TouchableOpacity&gt;`). Ensuite, dans le test, utiliser `by.id('loginButton')`. Cette approche est robuste car elle n'est pas affectée par les changements de texte, de style ou de structure. Les sélecteurs par texte (`by.text()`) sont fragiles (traduction, majuscules, caractères spéciaux)."
  }
]