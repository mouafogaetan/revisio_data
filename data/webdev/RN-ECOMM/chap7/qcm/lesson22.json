[
  {
    "questionText": "Dans React Native, quel composant est recommandé pour afficher une longue liste de produits avec chargement infini et performances optimales ?",
    "options": [
      "`ScrollView`",
      "`FlatList`",
      "`View` avec `map()`",
      "`SectionList`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`FlatList` est spécialement conçu pour les longues listes : il ne rend que les éléments visibles à l'écran (virtualisation), contrairement à `ScrollView` qui rend tout d'un coup et peut causer des ralentissements."
  },
  {
    "questionText": "Comment implémenter le chargement infini (pagination) avec `FlatList` dans React Native ?",
    "options": [
      "Utiliser la prop `onEndReached` avec `onEndReachedThreshold`",
      "Ajouter un `useEffect` qui écoute le scroll",
      "Utiliser `refreshControl` uniquement",
      "Appeler l'API dans `useFocusEffect`"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`onEndReached` est déclenché lorsque l'utilisateur atteint la fin de la liste. `onEndReachedThreshold` détermine à quelle distance de la fin le déclenchement a lieu (ex: 0.5 = à mi-chemin). C'est la méthode standard pour la pagination."
  },
  {
    "questionText": "Quelle bibliothèque est couramment utilisée dans l'écosystème Expo/React Native pour créer un `BottomSheet` modal servant de filtre ?",
    "options": [
      "`react-native-bottom-sheet`",
      "`@gorhom/bottom-sheet`",
      "`react-native-modal`",
      "`react-native-paper`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`@gorhom/bottom-sheet` est la bibliothèque la plus populaire pour les bottom sheets dans React Native. Elle supporte Expo, Reanimated 2, et offre des animations fluides avec snap points personnalisables."
  },
  {
    "questionText": "Comment mettre à jour dynamiquement les produits affichés après sélection de filtres (ex: catégorie, prix min/max) ?",
    "options": [
      "Recharger toute l'application",
      "Modifier l'état local `filters` et rappeler l'API avec les nouveaux paramètres",
      "Utiliser `useRef` pour forcer le re-rendu",
      "Redémarrer le `FlatList` avec `extraData`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "La bonne pratique est de stocker les filtres dans un état (useState ou Zustand), puis d'écouter les changements avec useEffect pour rappeler l'API avec les nouveaux paramètres (page=1, catégorie, prix...). `extraData` peut aussi forcer le re-rendu du FlatList."
  },
  {
    "questionText": "Quelle prop de `FlatList` permet d'afficher un indicateur de chargement en bas de liste pendant le chargement de la page suivante ?",
    "options": [
      "`ListFooterComponent`",
      "`ListHeaderComponent`",
      "`ItemSeparatorComponent`",
      "`ListEmptyComponent`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "`ListFooterComponent` s'affiche juste après le dernier élément. On y place un `ActivityIndicator` quand `isLoadingNextPage` est true. `ListHeaderComponent` est pour l'en-tête, `ListEmptyComponent` quand la liste est vide."
  },
  {
    "questionText": "Comment gérer le cas où l'utilisateur applique un filtre alors qu'une requête de pagination est déjà en cours ?",
    "options": [
      "Ignorer la nouvelle demande de filtre",
      "Annuler la requête précédente via `AbortController` et lancer la nouvelle",
      "Cumuler les deux requêtes en parallèle",
      "Désactiver le bouton de filtre pendant le chargement"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Pour éviter les incohérences (ex: résultats mélangés), on utilise `AbortController` pour annuler la requête précédente avant d'en lancer une nouvelle. On peut aussi désactiver les filtres pendant le chargement, mais l'annulation est plus élégante."
  },
  {
    "questionText": "Quelle structure de données est la plus adaptée pour stocker les résultats paginés d'une API dans un état React ?",
    "options": [
      "Un tableau simple écrasé à chaque chargement",
      "Un objet `{ data: [], nextPage: number, hasMore: boolean, isLoading: boolean }`",
      "Deux états séparés : `products` et `page`",
      "Un Map avec la page comme clé"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Un objet contenant `data` (tableau cumulé), `nextPage`, `hasMore` et `isLoading` est la meilleure pratique. Permet de concaténer les nouvelles pages, d'afficher un indicateur de fin, et d'éviter les appels redondants."
  },
  {
    "questionText": "Quel composant UI est recommandé pour implémenter une barre de recherche avec suggestions en temps réel sur mobile ?",
    "options": [
      "`TextInput` avec `onChangeText` + debounce et `FlatList` pour les suggestions",
      "`SearchBar` de React Native Elements uniquement",
      "`Modal` contenant un `Picker`",
      "`Button` qui ouvre une nouvelle page"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`TextInput` avec `onChangeText` associé à une fonction debounce (pour limiter les appels API) et une `FlatList` affichant les suggestions est la solution standard. React Native n'a pas de composant SearchBar natif."
  },
  {
    "questionText": "À quoi sert la prop `keyExtractor` dans `FlatList` ?",
    "options": [
      "Définir la clé unique de chaque élément pour optimiser le rendu",
      "Crypter les données de la liste",
      "Extraire les clés d'API des produits",
      "Filtrer les éléments en double"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "`keyExtractor` fournit une clé unique à chaque élément (généralement l'ID du produit). Cela permet à React Native d'identifier quels éléments ont changé, ajouté ou supprimé, optimisant ainsi les performances de la liste."
  },
  {
    "questionText": "Dans un écran de filtres avec Bottom Sheet, comment fermer le modal après validation des filtres ?",
    "options": [
      "Appeler `bottomSheetRef.current?.close()`",
      "Naviguer vers l'écran précédent",
      "Recharger l'application",
      "Modifier la variable `isVisible` à false"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "Avec `@gorhom/bottom-sheet`, on utilise une référence (`ref`) pour contrôler le Bottom Sheet. `bottomSheetRef.current?.close()` ferme le modal avec animation. La variable `isVisible` n'existe pas dans cette bibliothèque."
  },
  {
    "questionText": "Comment implémenter un tri des produits (ex: prix croissant, popularité) avec `FlatList` ?",
    "options": [
      "Trier les données avant de les passer à `FlatList`",
      "Utiliser la prop `sort` de `FlatList`",
      "Modifier l'ordre des éléments dans le DOM directement",
      "Re-rendre la liste avec des clés différentes"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`FlatList` n'a pas de prop `sort`. On trie les données dans l'état avant de les passer au composant. Ex: `const sortedData = [...data].sort((a,b) =&gt; a.price - b.price)` puis `data={sortedData}`."
  },
  {
    "questionText": "Quelle stratégie utilise-t-on pour éviter les appels API trop fréquents pendant la saisie dans une barre de recherche ?",
    "options": [
      "Appeler l'API à chaque frappe",
      "Utiliser un debounce (délai) avec `setTimeout` et `clearTimeout`",
      "Désactiver le TextInput après 3 caractères",
      "Utiliser un `Button` pour valider la recherche"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Le debounce consiste à attendre que l'utilisateur arrête de taper pendant X ms (ex: 300 ms) avant d'appeler l'API. `setTimeout` cumulé et `clearTimeout` à chaque frappe permet d'éviter des centaines d'appels inutiles."
  }
]