[
  {
    "questionText": "&lt;p&gt;Quelle méthode JavaScript permet de faire une requête HTTP pour récupérer des données depuis une API ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;axios.get()&lt;/code&gt;",
      "&lt;code&gt;fetch()&lt;/code&gt;",
      "&lt;code&gt;http.request()&lt;/code&gt;",
      "&lt;code&gt;XMLHttpRequest()&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "&lt;code&gt;fetch()&lt;/code&gt; est la méthode native moderne de JavaScript pour les requêtes réseau. Elle retourne une Promesse. Bien qu'axios et XMLHttpRequest fonctionnent aussi, fetch est intégré à React Native sans installation supplémentaire."
  },
  {
    "questionText": "&lt;p&gt;Dans un composant React Native, où doit-on placer l'appel à &lt;code&gt;fetch()&lt;/code&gt; pour charger des données au montage du composant ?&lt;/p&gt;",
    "options": [
      "Directement dans le corps du composant (render)",
      "Dans un &lt;code&gt;useEffect&lt;/code&gt; avec tableau de dépendances vide &lt;code&gt;[]&lt;/code&gt;",
      "Dans &lt;code&gt;useLayoutEffect&lt;/code&gt;",
      "Dans un gestionnaire d'événement &lt;code&gt;onPress&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Pour charger des données au montage d'un composant, on utilise &lt;code&gt;useEffect(() =&gt; { fetchData(); }, [])&lt;/code&gt;. Le tableau vide garantit que l'appel n'est effectué qu'une seule fois, après le premier rendu. L'appel direct dans le corps du composant serait exécuté à chaque re-rendu."
  },
  {
    "questionText": "&lt;p&gt;Soit le code suivant. Que va afficher l'application si la requête API échoue ?&lt;/p&gt;\n&lt;pre&gt;&lt;code&gt;\nconst [data, setData] = useState([]);\nconst [loading, setLoading] = useState(true);\nconst [error, setError] = useState(null);\n\nuseEffect(() =&gt; {\n  fetch('https://api.example.com/data')\n    .then(res =&gt; res.json())\n    .then(setData)\n    .catch(setError)\n    .finally(() =&gt; setLoading(false));\n}, []);\n\nif (loading) return &lt;Text&gt;Chargement...&lt;/Text&gt;;\nif (error) return &lt;Text&gt;Erreur : {error.message}&lt;/Text&gt;;\nreturn &lt;FlatList data={data} ... /&gt;;\n&lt;/code&gt;&lt;/pre&gt;",
    "options": [
      "Un écran vide",
      "Le texte 'Chargement...' indéfiniment",
      "Le texte 'Erreur : [message]'",
      "Le composant FlatList s'affiche avec des données vides"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Si la requête échoue, &lt;code&gt;catch(setError)&lt;/code&gt; est exécuté, stockant l'erreur dans l'état 'error'. Le composant affiche alors le message d'erreur. C'est une bonne pratique de gestion d'erreur pour informer l'utilisateur."
  },
  {
    "questionText": "&lt;p&gt;Pourquoi utilise-t-on &lt;code&gt;res.json()&lt;/code&gt; après un &lt;code&gt;fetch()&lt;/code&gt; ?&lt;/p&gt;",
    "options": [
      "Pour convertir la réponse en chaîne de caractères",
      "Pour parser la réponse au format JSON (objet JavaScript)",
      "Pour vérifier le statut HTTP de la réponse",
      "Pour fermer la connexion réseau"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La méthode &lt;code&gt;res.json()&lt;/code&gt; lit le corps de la réponse et le parse automatiquement en objet JavaScript. fetch retourne un objet Response qui contient le flux de données ; &lt;code&gt;json()&lt;/code&gt; est une méthode asynchrone qui extrait et parse ces données."
  },
  {
    "questionText": "&lt;p&gt;Quel est le rôle de la propriété &lt;code&gt;keyExtractor&lt;/code&gt; dans une FlatList ?&lt;/p&gt;",
    "options": [
      "Elle extrait la clé unique de chaque élément pour le rendu",
      "Elle trie les données de la liste",
      "Elle filtre les éléments de la liste",
      "Elle détermine la hauteur de chaque élément"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "&lt;code&gt;keyExtractor&lt;/code&gt; est une fonction qui reçoit chaque élément et retourne une chaîne de caractères unique servant de clé (key) pour React. Cela permet à FlatList d'optimiser le rendu et les mises à jour, similaire à l'attribut key dans React web."
  },
  {
    "questionText": "&lt;p&gt;Quelle est la bonne syntaxe pour afficher les données d'une API dans une FlatList ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;&lt;FlatList data={data} renderItem={({ item }) =&gt; &lt;Text&gt;{item.name}&lt;/Text&gt;} /&gt;&lt;/code&gt;",
      "&lt;code&gt;&lt;FlatList data={data} renderItem={(item) =&gt; &lt;Text&gt;{item.name}&lt;/Text&gt;} /&gt;&lt;/code&gt;",
      "&lt;code&gt;&lt;FlatList data={data} renderItem={item =&gt; &lt;Text&gt;{item.name}&lt;/Text&gt;} /&gt;&lt;/code&gt;",
      "Les réponses A et C sont correctes"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "La syntaxe correcte est &lt;code&gt;renderItem={({ item }) =&gt; ...}&lt;/code&gt; car renderItem reçoit un objet contenant la propriété 'item' (ainsi que 'index', 'separators'). Les options B et C ne déstructurent pas correctement l'objet et causeraient une erreur."
  },
  {
    "questionText": "&lt;p&gt;Comment gérer correctement une requête fetch avec annulation (abort) quand le composant est démonté avant la réponse ?&lt;/p&gt;",
    "options": [
      "Utiliser &lt;code&gt;AbortController&lt;/code&gt; et appeler &lt;code&gt;abort()&lt;/code&gt; dans la fonction de nettoyage de useEffect",
      "Ignorer le problème, ce n'est pas grave",
      "Utiliser &lt;code&gt;cancelFetch()&lt;/code&gt;",
      "Mettre la requête dans un &lt;code&gt;try/catch&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "L'utilisation d'&lt;code&gt;AbortController&lt;/code&gt; est la bonne pratique pour éviter les fuites mémoire et les appels à setState sur un composant démonté. Dans la fonction de nettoyage du useEffect, on appelle &lt;code&gt;abort()&lt;/code&gt;. Cela annule la requête en cours si le composant est démonté avant la réponse."
  },
  {
    "questionText": "&lt;p&gt;Que se passe-t-il si on omet la gestion de l'état 'loading' ?&lt;/p&gt;",
    "options": [
      "L'application crash",
      "Le composant affiche des données vides ou undefined temporairement, causant des erreurs",
      "Les données se chargent plus vite",
      "Rien, l'état loading est optionnel"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Sans gestion de loading, pendant que fetch s'exécute, 'data' est vide (son état initial). Le composant tente d'afficher des données undefined, ce qui peut provoquer des erreurs (ex: .map sur undefined). La meilleure pratique est d'afficher un indicateur de chargement tant que loading est true."
  },
  {
    "questionText": "&lt;p&gt;Lequel de ces schémas d'état est correct pour une récupération de données asynchrone ?&lt;/p&gt;",
    "options": [
      "loading → success → error",
      "error → loading → success",
      "loading → (success ou error)",
      "success → loading → error"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "L'état initial est 'loading' (chargement). Ensuite, selon l'issue : soit 'success' (données chargées), soit 'error' (erreur). On ne passe pas par 'error' avant 'loading' ni par 'success' avant 'loading'. C'est un pattern d'état standard pour les appels asynchrones."
  },
  {
    "questionText": "&lt;p&gt;Quelle propriété de FlatList permet d'afficher un composant pendant le chargement initial ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;ListEmptyComponent&lt;/code&gt;",
      "&lt;code&gt;ListHeaderComponent&lt;/code&gt;",
      "&lt;code&gt;ListFooterComponent&lt;/code&gt;",
      "&lt;code&gt;ListLoadingComponent&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;ListEmptyComponent&lt;/code&gt; s'affiche quand la liste est vide (data.length === 0). On peut l'utiliser pour afficher 'Chargement...' ou 'Aucune donnée'. Il n'y a pas de 'ListLoadingComponent' natif ; on gère généralement l'affichage du chargement en dehors de FlatList."
  },
  {
    "questionText": "&lt;p&gt;Comment tester le succès d'une requête fetch avant d'appeler &lt;code&gt;res.json()&lt;/code&gt; ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;if (res.ok)&lt;/code&gt;",
      "&lt;code&gt;if (res.status === 200)&lt;/code&gt;",
      "&lt;code&gt;if (res.success)&lt;/code&gt;",
      "Les deux premières réponses sont correctes"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;res.ok&lt;/code&gt; est un booléen qui vaut true si le statut HTTP est entre 200 et 299. On peut aussi vérifier &lt;code&gt;res.status === 200&lt;/code&gt;. C'est une bonne pratique pour gérer les erreurs HTTP (404, 500, etc.) avant de tenter de parser la réponse."
  },
  {
    "questionText": "&lt;p&gt;Dans une FlatList, que signifie l'avertissement 'VirtualizedList: missing keys for items' ?&lt;/p&gt;",
    "options": [
      "Il manque la prop &lt;code&gt;keyExtractor&lt;/code&gt; ou elle ne retourne pas de valeur unique",
      "Les données sont mal formatées",
      "La liste est trop grande",
      "Il faut utiliser ScrollView à la place"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Cet avertissement apparaît quand FlatList ne peut pas identifier de manière unique chaque élément. La solution est de fournir une &lt;code&gt;keyExtractor&lt;/code&gt; qui retourne un identifiant unique pour chaque élément (ex: item.id). Sans cela, les performances et le comportement des mises à jour peuvent être dégradés."
  },
  {
    "questionText": "&lt;p&gt;Quelle est la structure de données typique pour stocker le résultat d'un fetch dans un état ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;useState([])&lt;/code&gt; pour un tableau, &lt;code&gt;useState(null)&lt;/code&gt; pour un objet",
      "&lt;code&gt;useState({})&lt;/code&gt; pour toutes les données",
      "&lt;code&gt;useState('')&lt;/code&gt; pour les chaînes JSON",
      "&lt;code&gt;useState(0)&lt;/code&gt; pour les compteurs"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "Si l'API retourne une liste, on initialise l'état avec un tableau vide &lt;code&gt;[]&lt;/code&gt;. Si l'API retourne un objet (ex: { user: ... }), on initialise avec &lt;code&gt;null&lt;/code&gt;. Cela permet de faire des vérifications conditionnelles : &lt;code&gt;if (data &amp;&amp; data.user)&lt;/code&gt;."
  },
  {
    "questionText": "&lt;p&gt;Comment éviter d'appeler fetch plusieurs fois en développement avec React.StrictMode ?&lt;/p&gt;",
    "options": [
      "Désactiver StrictMode",
      "Utiliser une ref pour suivre si fetch a déjà été appelé",
      "Utiliser &lt;code&gt;useEffect(() =&gt; {...}, [])&lt;/code&gt; avec un flag &lt;code&gt;useRef(false)&lt;/code&gt;",
      "Les deux réponses B et C sont valables"
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "En développement avec StrictMode, useEffect s'exécute deux fois pour détecter les effets non nettoyés. On peut utiliser une ref : &lt;code&gt;const fetched = useRef(false); if (!fetched.current) { fetch(); fetched.current = true; }&lt;/code&gt; pour n'exécuter le fetch qu'une seule fois. En production, cela n'est pas nécessaire car StrictMode ne s'applique pas."
  },
  {
    "questionText": "&lt;p&gt;Quel est l'avantage de FlatList par rapport à ScrollView pour l'affichage de données d'API ?&lt;/p&gt;",
    "options": [
      "FlatList est plus simple à utiliser",
      "FlatList effectue un rendu virtuel : ne rend que les éléments visibles à l'écran, économisant la mémoire",
      "FlatList supporte le chargement infini nativement",
      "FlatList gère automatiquement le caching des données"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "FlatList utilise le 'windowed rendering' : seuls les éléments visibles (plus une marge) sont rendus. Pour une grande liste (ex: 1000 éléments), ScrollView rendrait tous les éléments, ce qui serait très lent. FlatList est donc beaucoup plus performant pour les listes de données provenant d'API."
  },
  {
    "questionText": "&lt;p&gt;Comment implémenter un bouton 'Retry' après une erreur de chargement ?&lt;/p&gt;",
    "options": [
      "Créer une fonction &lt;code&gt;refetch&lt;/code&gt; qui réinitialise les états et rappelle fetch, et l'appeler dans &lt;code&gt;onPress&lt;/code&gt;",
      "Recharger toute l'application",
      "Utiliser &lt;code&gt;window.location.reload()&lt;/code&gt;",
      "C'est impossible, l'utilisateur doit redémarrer l'app"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "On crée une fonction &lt;code&gt;refetch&lt;/code&gt; qui remet loading à true, error à null, et exécute à nouveau fetch. On affiche un bouton 'Réessayer' dans l'écran d'erreur, avec &lt;code&gt;onPress={refetch}&lt;/code&gt;. C'est une excellente UX."
  },
  {
    "questionText": "&lt;p&gt;Que signifie l'erreur 'Network request failed' dans React Native ?&lt;/p&gt;",
    "options": [
      "L'URL de l'API est mal écrite",
      "Le serveur est inaccessible (pas d'internet, serveur down, erreur DNS)",
      "Le format JSON est invalide",
      "La requête a pris trop de temps"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Cette erreur apparaît généralement quand le réseau n'est pas disponible ou que l'URL ne peut être atteinte (ex: serveur hors ligne, problèmes de connectivité, erreur de certificat SSL). C'est différent d'une erreur HTTP 404 ou 500 qui renvoie un statut mais la requête aboutit."
  },
  {
    "questionText": "&lt;p&gt;Quel est le bon ordre des imports et des déclarations pour un composant qui utilise fetch ?&lt;/p&gt;",
    "options": [
      "imports → état (useState) → useEffect → définition de la fonction fetch → return",
      "imports → définition de la fonction fetch → état → useEffect → return",
      "imports → état → définition de la fonction fetch → useEffect → return",
      "Peu importe l'ordre"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "L'ordre classique : 1) imports, 2) déclaration des useState (états), 3) définition de la fonction asynchrone fetchData, 4) useEffect qui appelle fetchData, 5) return du JSX. La fonction fetchData doit être définie avant son appel dans useEffect pour éviter les erreurs (ou être définie à l'intérieur du useEffect)."
  },
  {
    "questionText": "&lt;p&gt;À quoi sert &lt;code&gt;finally(() =&gt; setLoading(false))&lt;/code&gt; ?&lt;/p&gt;",
    "options": [
      "À exécuter du code uniquement en cas de succès",
      "À exécuter du code uniquement en cas d'erreur",
      "À exécuter du code dans les deux cas (succès ou erreur), ici pour arrêter l'indicateur de chargement",
      "À annuler la requête"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "&lt;code&gt;finally&lt;/code&gt; s'exécute toujours, que la promesse soit résolue ou rejetée. C'est l'endroit idéal pour désactiver l'indicateur de chargement, car on ne veut plus qu'il s'affiche une fois la requête terminée (quelle que soit l'issue)."
  },
  {
    "questionText": "&lt;p&gt;Comment afficher un message 'Aucune donnée' quand l'API retourne un tableau vide ?&lt;/p&gt;",
    "options": [
      "Utiliser &lt;code&gt;ListEmptyComponent&lt;/code&gt; dans FlatList",
      "Vérifier &lt;code&gt;if (data.length === 0) return &lt;Text&gt;Aucune donnée&lt;/Text&gt;&lt;/code&gt;",
      "Afficher un message d'erreur",
      "Les réponses A et B sont toutes deux valables"
    ],
    "correctAnswer": 3,
    "difficulty": "easy",
    "explanation": "Les deux méthodes fonctionnent : 1) conditionnelle dans le rendu, 2) prop &lt;code&gt;ListEmptyComponent&lt;/code&gt; de FlatList (plus élégante car intégrée). &lt;code&gt;ListEmptyComponent&lt;/code&gt; s'affiche automatiquement quand data est un tableau vide."
  }
]