[
  {
    "questionText": "&lt;p&gt;Quelle commande npm permet d'installer &lt;code&gt;socket.io&lt;/code&gt; côté serveur dans un projet Node.js ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;npm install socket&lt;/code&gt;",
      "&lt;code&gt;npm install websocket&lt;/code&gt;",
      "&lt;code&gt;npm install socket.io&lt;/code&gt;",
      "&lt;code&gt;npm install ws&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La bonne commande est &lt;code&gt;npm install socket.io&lt;/code&gt;. Socket.io est la bibliothèque standard pour la communication temps réel bidirectionnelle entre client et serveur. Les autres commandes installent des bibliothèques différentes (ws est une autre bibliothèque WebSocket, socket et websocket ne sont pas les bons packages)."
  },
  {
    "questionText": "&lt;p&gt;Pour initialiser un serveur Socket.io sur un serveur Express existant, quelle syntaxe est correcte ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;const io = socket.io(server);&lt;/code&gt;",
      "&lt;code&gt;const io = require('socket.io')(server);&lt;/code&gt;",
      "&lt;code&gt;const io = new Socket(server);&lt;/code&gt;",
      "&lt;code&gt;const io = require('socket').attach(server);&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La syntaxe correcte est &lt;code&gt;const io = require('socket.io')(server);&lt;/code&gt; où server est l'instance du serveur HTTP (créé par Express ou http). Cette syntaxe attache Socket.io au serveur HTTP existant. Les autres propositions utilisent des noms ou méthodes incorrects."
  },
  {
    "questionText": "&lt;p&gt;Quelle méthode côté serveur permet d'écouter un événement envoyé par le client ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;socket.send()&lt;/code&gt;",
      "&lt;code&gt;socket.emit()&lt;/code&gt;",
      "&lt;code&gt;socket.on()&lt;/code&gt;",
      "&lt;code&gt;socket.listen()&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La méthode &lt;code&gt;socket.on('nomEvenement', callback)&lt;/code&gt; permet d'écouter (recevoir) un événement émis par le client. &lt;code&gt;socket.emit()&lt;/code&gt; sert à émettre un événement vers le client, &lt;code&gt;socket.send()&lt;/code&gt; est une méthode plus ancienne pour envoyer des messages bruts."
  },
  {
    "questionText": "&lt;p&gt;Quelle méthode côté client (navigateur) permet d'envoyer un événement au serveur avec Socket.io ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;socket.send()&lt;/code&gt;",
      "&lt;code&gt;socket.emit()&lt;/code&gt;",
      "&lt;code&gt;socket.on()&lt;/code&gt;",
      "&lt;code&gt;socket.write()&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Côté client, &lt;code&gt;socket.emit('nomEvenement', données)&lt;/code&gt; est utilisée pour envoyer un événement au serveur. &lt;code&gt;socket.on()&lt;/code&gt; est utilisé pour recevoir des événements du serveur. Les autres méthodes ne font pas partie de l'API standard de Socket.io."
  },
  {
    "questionText": "&lt;p&gt;Quelle est la différence principale entre un &lt;strong&gt;namespace&lt;/strong&gt; et une &lt;strong&gt;room&lt;/strong&gt; dans Socket.io ?&lt;/p&gt;",
    "options": [
      "Les namespaces sont créés par les clients, les rooms par le serveur",
      "Un namespace est un canal de communication global (ex: /admin) tandis qu'une room est un sous-groupe à l'intérieur d'un namespace",
      "Les rooms sont persistantes, les namespaces sont temporaires",
      "Il n'y a aucune différence, ce sont des synonymes"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Un namespace est un canal de communication séparé accessible via une URL différente (ex: &lt;code&gt;/chat&lt;/code&gt;, &lt;code&gt;/admin&lt;/code&gt;). Chaque namespace a sa propre connexion. Une room est un sous-groupe à l'intérieur d'un namespace, permettant d'envoyer des messages à un ensemble spécifique de clients (ex: une salle de discussion). On peut avoir plusieurs rooms par namespace."
  },
  {
    "questionText": "&lt;p&gt;Comment un client se connecte-t-il à un namespace spécifique &lt;code&gt;/admin&lt;/code&gt; ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;io.connect('/admin')&lt;/code&gt;",
      "&lt;code&gt;io.of('/admin').connect()&lt;/code&gt;",
      "&lt;code&gt;io.socket('/admin')&lt;/code&gt;",
      "&lt;code&gt;new io('/admin')&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Côté client, on utilise &lt;code&gt;io.connect('/admin')&lt;/code&gt; ou &lt;code&gt;io('/admin')&lt;/code&gt; pour se connecter à un namespace autre que le namespace par défaut (&lt;code&gt;/&lt;/code&gt;). La méthode &lt;code&gt;io.of('/admin')&lt;/code&gt; est utilisée côté serveur pour créer un namespace."
  },
  {
    "questionText": "&lt;p&gt;Côté serveur, comment envoyer un message à tous les clients d'une room appelée &lt;code&gt;\"salle1\"&lt;/code&gt; ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;io.emit('message', data)&lt;/code&gt;",
      "&lt;code&gt;socket.to('salle1').emit('message', data)&lt;/code&gt;",
      "&lt;code&gt;io.to('salle1').emit('message', data)&lt;/code&gt;",
      "&lt;code&gt;io.in('salle1').send('message', data)&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "La méthode correcte est &lt;code&gt;io.to('salle1').emit('message', data)&lt;/code&gt; ou &lt;code&gt;io.in('salle1').emit('message', data)&lt;/code&gt; (to et in sont équivalents). &lt;code&gt;socket.to('salle1')&lt;/code&gt; enverrait uniquement aux autres clients de la salle (sauf l'émetteur), mais généralement on utilise &lt;code&gt;io.to()&lt;/code&gt; pour tous les membres de la room."
  },
  {
    "questionText": "&lt;p&gt;Quel événement est déclenché côté serveur lorsqu'un client se connecte ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;'connect'&lt;/code&gt;",
      "&lt;code&gt;'connection'&lt;/code&gt;",
      "&lt;code&gt;'clientConnect'&lt;/code&gt;",
      "&lt;code&gt;'join'&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Côté serveur, l'événement écouté est &lt;code&gt;io.on('connection', (socket) =&gt; { ... })&lt;/code&gt;. &lt;code&gt;'connect'&lt;/code&gt; est utilisé côté client. Les autres noms ne sont pas des événements standards de Socket.io."
  },
  {
    "questionText": "&lt;p&gt;Comment un client rejoint-il une room appelée &lt;code&gt;\"gameRoom\"&lt;/code&gt; ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;socket.join('gameRoom')&lt;/code&gt; (côté serveur uniquement)",
      "&lt;code&gt;client.join('gameRoom')&lt;/code&gt; (côté client)",
      "&lt;code&gt;socket.emit('join', 'gameRoom')&lt;/code&gt; (côté client)",
      "&lt;code&gt;io.join('gameRoom')&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "La méthode &lt;code&gt;socket.join('gameRoom')&lt;/code&gt; est exécutée côté serveur. C'est au serveur de gérer l'appartenance aux rooms, généralement en réponse à un événement émis par le client (ex: &lt;code&gt;socket.on('joinGame', () =&gt; socket.join('gameRoom'))&lt;/code&gt;). Le client ne peut pas rejoindre une room directement."
  },
  {
    "questionText": "&lt;p&gt;Quelle propriété permet d'obtenir un identifiant unique pour chaque socket ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;socket.id&lt;/code&gt;",
      "&lt;code&gt;socket.uuid&lt;/code&gt;",
      "&lt;code&gt;socket.clientId&lt;/code&gt;",
      "&lt;code&gt;socket.sessionId&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "Chaque socket possède une propriété &lt;code&gt;socket.id&lt;/code&gt; qui est un identifiant unique généré automatiquement. Cet identifiant permet de cibler un client spécifique pour l'envoi de messages privés."
  },
  {
    "questionText": "&lt;p&gt;Pour envoyer un message privé à un client spécifique identifié par son &lt;code&gt;socket.id&lt;/code&gt;, quelle méthode utilise-t-on ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;io.to(socketId).emit('message', data)&lt;/code&gt;",
      "&lt;code&gt;socket.to(socketId).send(data)&lt;/code&gt;",
      "&lt;code&gt;io.client(socketId).emit('message', data)&lt;/code&gt;",
      "&lt;code&gt;socket.broadcast.to(socketId).emit(data)&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Pour envoyer un message à un client spécifique, on utilise &lt;code&gt;io.to(socketId).emit('message', data)&lt;/code&gt;. Cette méthode utilise le mécanisme des rooms (chaque socket appartient automatiquement à une room portant son ID). &lt;code&gt;socket.to(socketId)&lt;/code&gt; enverrait aux autres clients sauf l'émetteur."
  },
  {
    "questionText": "&lt;p&gt;Quel est le rôle de &lt;code&gt;socket.broadcast.emit('event', data)&lt;/code&gt; côté serveur ?&lt;/p&gt;",
    "options": [
      "Envoyer un message à tous les clients, y compris l'émetteur",
      "Envoyer un message à tous les clients SAUF l'émetteur",
      "Envoyer un message uniquement à l'émetteur",
      "Diffuser un message dans tous les namespaces"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;socket.broadcast.emit()&lt;/code&gt; permet d'envoyer un message à tous les clients connectés au namespace, sauf le socket qui émet le broadcast. C'est utile pour annoncer une action (ex: \"un utilisateur a quitté le chat\") sans que l'émetteur reçoive sa propre notification."
  },
  {
    "questionText": "&lt;p&gt;Quel événement est automatiquement déclenché lorsqu'un client se déconnecte ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;'disconnect'&lt;/code&gt;",
      "&lt;code&gt;'close'&lt;/code&gt;",
      "&lt;code&gt;'leave'&lt;/code&gt;",
      "&lt;code&gt;'end'&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "L'événement &lt;code&gt;'disconnect'&lt;/code&gt; est automatiquement émis par le serveur lorsqu'un client se déconnecte (fermeture du navigateur, perte de connexion, etc.). On peut l'écouter avec &lt;code&gt;socket.on('disconnect', () =&gt; { ... })&lt;/code&gt; pour nettoyer des ressources ou informer les autres utilisateurs."
  },
  {
    "questionText": "&lt;p&gt;Que se passe-t-il si un client tente de se connecter avec une version de Socket.io incompatible avec celle du serveur ?&lt;/p&gt;",
    "options": [
      "La connexion échoue silencieusement",
      "Le serveur se bloque",
      "Une erreur est levée et la connexion est automatiquement rétablie avec la bonne version",
      "Le serveur tente une mise à jour automatique de la bibliothèque cliente"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "Si les versions de Socket.io entre client et serveur sont incompatibles (généralement des différences majeures), la connexion échoue silencieusement ou avec une erreur dans la console. Il n'y a pas de mise à jour automatique. Il est crucial de maintenir des versions compatibles (ex: client 4.x avec serveur 4.x)."
  },
  {
    "questionText": "&lt;p&gt;Parmi ces propositions, laquelle permet de créer un namespace &lt;code&gt;/chat&lt;/code&gt; côté serveur ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;const chatNamespace = io.namespace('/chat');&lt;/code&gt;",
      "&lt;code&gt;const chatNamespace = io.of('/chat');&lt;/code&gt;",
      "&lt;code&gt;const chatNamespace = io.route('/chat');&lt;/code&gt;",
      "&lt;code&gt;const chatNamespace = io.createNamespace('/chat');&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "La méthode &lt;code&gt;io.of('/chat')&lt;/code&gt; est utilisée côté serveur pour créer ou récupérer un namespace. On peut ensuite écouter les connexions sur ce namespace avec &lt;code&gt;chatNamespace.on('connection', ...)&lt;/code&gt;. Les autres propositions n'existent pas dans l'API Socket.io."
  },
  {
    "questionText": "&lt;p&gt;Quelle option de configuration permet d'activer la reconnexion automatique côté client Socket.io ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;{ autoConnect: false }&lt;/code&gt;",
      "&lt;code&gt;{ reconnection: true }&lt;/code&gt; (activé par défaut)",
      "&lt;code&gt;{ retry: 5 }&lt;/code&gt;",
      "&lt;code&gt;{ reconnectInterval: 1000 }&lt;/code&gt; n'existe pas"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "La reconnexion automatique est activée par défaut côté client. On peut la désactiver avec &lt;code&gt;{ autoConnect: false }&lt;/code&gt; ou la configurer avec des options comme &lt;code&gt;reconnectionAttempts&lt;/code&gt; et &lt;code&gt;reconnectionDelay&lt;/code&gt;. &lt;code&gt;reconnection: true&lt;/code&gt; est bien l'option (bien que par défaut elle soit déjà true)."
  },
  {
    "questionText": "&lt;p&gt;Quelle méthode permet de quitter une room côté serveur ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;socket.leave('roomName')&lt;/code&gt;",
      "&lt;code&gt;socket.exit('roomName')&lt;/code&gt;",
      "&lt;code&gt;socket.remove('roomName')&lt;/code&gt;",
      "&lt;code&gt;socket.disconnectRoom('roomName')&lt;/code&gt;"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "La méthode &lt;code&gt;socket.leave('roomName')&lt;/code&gt; permet à un socket de quitter une room à laquelle il avait précédemment adhéré. C'est la contrepartie de &lt;code&gt;socket.join()&lt;/code&gt;. Les autres méthodes n'existent pas dans l'API standard."
  },
  {
    "questionText": "&lt;p&gt;Dans un contexte de chat avec Socket.io, que signifie l'événement &lt;code&gt;'typing'&lt;/code&gt; personnalisé ?&lt;/p&gt;",
    "options": [
      "C'est un événement natif de Socket.io",
      "C'est un événement personnalisé que le développeur crée pour indiquer qu'un utilisateur est en train d'écrire",
      "C'est un événement automatique pour la gestion des erreurs",
      "C'est un événement uniquement disponible côté serveur"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Socket.io permet de créer des événements personnalisés. &lt;code&gt;'typing'&lt;/code&gt; n'est pas natif ; c'est une convention utilisée par les développeurs pour indiquer qu'un utilisateur est en train de taper un message. On peut émettre cet événement côté client et l'écouter côté serveur ou inversement."
  },
  {
    "questionText": "&lt;p&gt;Comment obtenir le nombre de clients connectés dans une room spécifique ?&lt;/p&gt;",
    "options": [
      "&lt;code&gt;io.in('roomName').clientsCount&lt;/code&gt;",
      "&lt;code&gt;io.sockets.adapter.rooms.get('roomName')?.size&lt;/code&gt;",
      "&lt;code&gt;io.room('roomName').size()&lt;/code&gt;",
      "&lt;code&gt;socket.getRoomSize('roomName')&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "On peut accéder à l'adaptateur Socket.io pour obtenir la taille d'une room : &lt;code&gt;io.sockets.adapter.rooms.get('roomName')?.size&lt;/code&gt; (en asynchrone, on peut aussi utiliser &lt;code&gt;io.in('roomName').fetchSockets()&lt;/code&gt; puis &lt;code&gt;.length&lt;/code&gt;). Les autres syntaxes ne sont pas valides."
  },
  {
    "questionText": "&lt;p&gt;Quel transport par défaut Socket.io utilise-t-il prioritairement ?&lt;/p&gt;",
    "options": [
      "HTTP long-polling uniquement",
      "WebSocket (puis fallback sur HTTP long-polling)",
      "WebRTC",
      "Server-Sent Events (SSE)"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Socket.io utilise WebSocket comme transport principal car il permet une communication bidirectionnelle en temps réel. Si WebSocket n'est pas disponible (pare-feu, proxy), il bascule automatiquement sur le HTTP long-polling pour assurer la compatibilité maximale."
  }
]