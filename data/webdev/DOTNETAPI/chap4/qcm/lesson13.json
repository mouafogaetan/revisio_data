[
  {
    "questionText": "En Entity Framework Core, quelle méthode permet de récupérer &lt;strong&gt;tous&lt;/strong&gt; les enregistrements d'une table sans aucune condition &lt;code&gt;WHERE&lt;/code&gt; ?",
    "options": [
      "&lt;code&gt;FindAsync()&lt;/code&gt;",
      "&lt;code&gt;FirstOrDefault()&lt;/code&gt;",
      "&lt;code&gt;ToListAsync()&lt;/code&gt;",
      "&lt;code&gt;SingleAsync()&lt;/code&gt;"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La méthode &lt;code&gt;ToListAsync()&lt;/code&gt; exécute la requête et matérialise tous les résultats sous forme de liste. &lt;code&gt;FindAsync()&lt;/code&gt; cherche par clé primaire, &lt;code&gt;FirstOrDefault()&lt;/code&gt; retourne le premier élément (ou null), et &lt;code&gt;SingleAsync()&lt;/code&gt; attend un seul élément, sinon lève une exception."
  },
  {
    "questionText": "Quelle est la différence fondamentale entre &lt;code&gt;FirstOrDefault()&lt;/code&gt; et &lt;code&gt;Find()&lt;/code&gt; sur un &lt;code&gt;DbSet&lt;T&gt;&lt;/code&gt; ?",
    "options": [
      "Aucune différence, ce sont des alias",
      "&lt;code&gt;Find()&lt;/code&gt; cherche uniquement en mémoire tandis que &lt;code&gt;FirstOrDefault()&lt;/code&gt; va toujours en base",
      "&lt;code&gt;Find()&lt;/code&gt; cherche d'abord dans le cache local (Change Tracker) avant d'aller en base, &lt;code&gt;FirstOrDefault()&lt;/code&gt; exécute toujours une requête SQL",
      "&lt;code&gt;FirstOrDefault()&lt;/code&gt; est asynchrone, &lt;code&gt;Find()&lt;/code&gt; est synchrone"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;Find()&lt;/code&gt; vérifie d'abord si l'entité avec la clé primaire donnée est déjà suivie par le &lt;code&gt;DbContext&lt;/code&gt; (cache). Si oui, il la retourne sans requête SQL. Sinon, il interroge la base. &lt;code&gt;FirstOrDefault()&lt;/code&gt; exécute systématiquement une requête SQL même si l'objet est déjà en mémoire."
  },
  {
    "questionText": "Soit le code suivant : &lt;br&gt;&lt;code&gt;var produit = context.Produits.Find(42);&lt;/code&gt;&lt;br&gt;&lt;code&gt;produit.Prix = 99.99m;&lt;/code&gt;&lt;br&gt;&lt;code&gt;await context.SaveChangesAsync();&lt;/code&gt;&lt;br&gt;Que se passe-t-il ?",
    "options": [
      "Une erreur car il faut appeler &lt;code&gt;Update()&lt;/code&gt; avant &lt;code&gt;SaveChangesAsync()&lt;/code&gt;",
      "L'enregistrement avec l'ID 42 est modifié en base avec le nouveau prix",
      "Seule l'entité en mémoire est modifiée, la base reste inchangée",
      "Une nouvelle ligne est insérée dans la table"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Entity Framework Core suit automatiquement les entités récupérées via le &lt;code&gt;DbContext&lt;/code&gt;. &lt;code&gt;Find()&lt;/code&gt; ou toute requête de sélection attache l'entité au Change Tracker avec l'état &lt;code&gt;Unchanged&lt;/code&gt;. Toute modification de propriété passe l'état à &lt;code&gt;Modified&lt;/code&gt;, et &lt;code&gt;SaveChangesAsync()&lt;/code&gt; génère un &lt;code&gt;UPDATE SQL&lt;/code&gt;."
  },
  {
    "questionText": "Quelle est la manière correcte d'ajouter une nouvelle entité &lt;code&gt;Article&lt;/code&gt; de façon asynchrone ?",
    "options": [
      "&lt;code&gt;context.Articles.Add(newArticle); await context.SaveChangesAsync();&lt;/code&gt;",
      "&lt;code&gt;await context.Articles.AddAsync(newArticle); await context.SaveChangesAsync();&lt;/code&gt;",
      "&lt;code&gt;context.Articles.Add(newArticle); context.SaveChanges();&lt;/code&gt;",
      "&lt;code&gt;context.Articles.Insert(newArticle); context.SubmitChanges();&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La méthode &lt;code&gt;AddAsync()&lt;/code&gt; est recommandée pour les opérations asynchrones car elle génère des valeurs (comme les clés générées par la base) de manière non bloquante. &lt;code&gt;Add()&lt;/code&gt; synchrone existe aussi mais n'est pas optimale dans un contexte async. L'appel à &lt;code&gt;SaveChangesAsync()&lt;/code&gt; est essentiel pour persister réellement les changements."
  },
  {
    "questionText": "Pour supprimer un produit dont l'ID est 10, sans le charger préalablement en mémoire, quelle approche est la plus performante ?",
    "options": [
      "&lt;code&gt;var p = context.Produits.Find(10); context.Produits.Remove(p); await context.SaveChangesAsync();&lt;/code&gt;",
      "&lt;code&gt;var p = new Produit { Id = 10 }; context.Produits.Attach(p); context.Produits.Remove(p); await context.SaveChangesAsync();&lt;/code&gt;",
      "&lt;code&gt;await context.Produits.Where(p =&gt; p.Id == 10).DeleteAsync();&lt;/code&gt; (si package tiers autorisé)",
      "&lt;code&gt;context.Database.ExecuteSqlRaw(\"DELETE FROM Produits WHERE Id = 10\");&lt;/code&gt;"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "L'approche la plus performante sans requête SELECT préalable est de créer une entité factice (avec l'ID uniquement), l'attacher au contexte en état &lt;code&gt;Unchanged&lt;/code&gt;, puis appeler &lt;code&gt;Remove()&lt;/code&gt; (qui passe l'état à &lt;code&gt;Deleted&lt;/code&gt;). Option 3 (ExecuteDelete) existe en EF Core 7+ mais n'est pas standard dans les objectifs de la leçon. L'option 0 fait un SELECT inutile."
  },
  {
    "questionText": "Que retourne la méthode &lt;code&gt;SaveChangesAsync()&lt;/code&gt; ?",
    "options": [
      "Le nombre total d'entités suivies par le &lt;code&gt;DbContext&lt;/code&gt;",
      "Le nombre d'enregistrements (lignes) affectés en base de données",
      "Un booléen indiquant si la sauvegarde a réussi",
      "La liste des clés primaires des entités modifiées"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "&lt;code&gt;SaveChangesAsync()&lt;/code&gt; retourne un &lt;code&gt;int&lt;/code&gt; représentant le nombre d'états d'entités convertis en commandes SQL qui ont affecté une ou plusieurs lignes. Par exemple, une insertion (1 ligne) retourne 1, une mise à jour retourne 1, une suppression retourne 1. Cela permet de vérifier qu'une opération a bien eu lieu."
  },
  {
    "questionText": "Quelle méthode faut-il utiliser pour mettre à jour une entité détachée (non suivie par le contexte) reçue depuis un client HTTP ?",
    "options": [
      "&lt;code&gt;context.Update(entiteDetachee); await context.SaveChangesAsync();&lt;/code&gt;",
      "&lt;code&gt;context.Attach(entiteDetachee); context.Entry(entiteDetachee).State = EntityState.Modified; await context.SaveChangesAsync();&lt;/code&gt;",
      "&lt;code&gt;context.ChangeTracker.TrackGraph(entiteDetachee, ...);&lt;/code&gt;",
      "Les deux premières réponses sont correctes selon le scénario"
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "Les deux premières sont valides : &lt;code&gt;Update()&lt;/code&gt; marque toutes les propriétés comme modifiées (génère un UPDATE sur toutes les colonnes). La seconde méthode permet de marquer uniquement certaines propriétés modifiées pour un UPDATE partiel. Le choix dépend si on veut écraser toutes les valeurs ou seulement certaines."
  },
  {
    "questionText": "Dans une API REST, pourquoi est-il important d'utiliser &lt;code&gt;AddAsync()&lt;/code&gt; et &lt;code&gt;SaveChangesAsync()&lt;/code&gt; plutôt que leurs versions synchrones ?",
    "options": [
      "Pour éviter les deadlocks et améliorer la scalabilité du serveur web",
      "Parce que Entity Framework Core n'offre pas de méthodes synchrones",
      "Uniquement pour respecter une convention de nommage",
      "Les méthodes async sont plus rapides en termes de temps d'exécution pur"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Dans un contexte serveur (ASP.NET Core), les opérations asynchrones libèrent le thread pendant les I/O (accès disque/réseau de la base), permettant au serveur de traiter d'autres requêtes simultanément. Cela améliore la scalabilité et évite l'épuisement du pool de threads."
  }
]