[
  {
    "questionText": "Quel package NestJS doit-on installer pour gérer facilement les variables d'environnement ?",
    "options": [
      "`@nestjs/env`",
      "`@nestjs/config`",
      "`@nestjs/dotenv`",
      "`@nestjs/environment`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Le package officiel est `@nestjs/config`. Il fournit un module qui charge les variables depuis un fichier `.env` et les rend accessibles via `ConfigService`."
  },
  {
    "questionText": "Quelle est l'extension standard pour le fichier contenant les variables d'environnement en développement ?",
    "options": [
      "`.env.prod`",
      "`.env`",
      "`.env.local`",
      "`.config.json`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Par convention, le fichier de base est `.env`. On peut avoir `.env.development`, `.env.staging`, `.env.production` pour les différents environnements."
  },
  {
    "questionText": "Comment accède-t-on à une variable d'environnement après avoir configuré `ConfigModule` dans NestJS ?",
    "options": [
      "`process.env.MA_VARIABLE`",
      "`this.configService.get('MA_VARIABLE')`",
      "`this.env.get('MA_VARIABLE')`",
      "`ConfigService.getEnv('MA_VARIABLE')`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Après injection du `ConfigService`, on utilise la méthode `get()` : `this.configService.get('MA_VARIABLE')`. Bien que `process.env` fonctionne encore, la bonne pratique NestJS est d'utiliser le service."
  },
  {
    "questionText": "Quel package est recommandé pour valider le schéma des variables d'environnement dans une application NestJS ?",
    "options": [
      "`class-validator`",
      "`joi`",
      "`yup`",
      "`zod`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "NestJS recommande officiellement `joi` pour la validation des variables d'environnement. On peut l'utiliser avec l'option `validationSchema` du `ConfigModule`."
  },
  {
    "questionText": "Voici un extrait de code. Quelle ligne permet de charger un fichier `.env.staging` spécifiquement ?\n\n```typescript\n@Module({\n  imports: [\n    ConfigModule.forRoot({\n      ???\n    })\n  ]\n})\nexport class AppModule {}\n```",
    "options": [
      "`envFilePath: '.env.staging'`",
      "`path: '.env.staging'`",
      "`envFile: '.env.staging'`",
      "`filePath: '.env.staging'`"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "L'option `envFilePath` permet de spécifier le chemin du fichier `.env` à charger. Exemple : `envFilePath: '.env.staging'`. On peut aussi passer un tableau de fichiers."
  },
  {
    "questionText": "Quel est l'avantage d'utiliser `ConfigModule.forRoot({ isGlobal: true })` ?",
    "options": [
      "Le module devient accessible dans tous les modules sans importation manuelle",
      "Les variables deviennent immutables",
      "Les variables sont automatiquement chiffrées",
      "Le module est automatiquement testé"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "`isGlobal: true` rend le `ConfigModule` global, ce qui évite de devoir l'importer dans chaque module de l'application. Le `ConfigService` est alors disponible partout."
  },
  {
    "questionText": "Comment ignorer le fichier `.env` dans Git pour éviter de commettre des secrets ?",
    "options": [
      "Ajouter `.env` dans le fichier `.dockerignore`",
      "Ajouter `.env` dans le fichier `.gitignore`",
      "Ajouter `secrets: true` dans `tsconfig.json`",
      "Utiliser `ConfigModule.forRoot({ ignoreEnvFile: true })`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Pour éviter de commit des secrets (clés API, mots de passe), on ajoute `.env` (ou `.env.*`) dans le fichier `.gitignore`. Le fichier d'exemple `.env.example` est souvent versionné."
  },
  {
    "questionText": "Avec `@nestjs/config`, comment charger à la fois un fichier `.env` par défaut ET un fichier spécifique à l'environnement (ex: `.env.production`) ?",
    "options": [
      "`envFilePath: ['.env', '.env.production']`",
      "`files: ['.env', '.env.production']`",
      "`load: ['.env', '.env.production']`",
      "`envFilePaths: ['.env', '.env.production']`"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "L'option `envFilePath` accepte un tableau de chemins. Les valeurs du dernier fichier écrasent celles des précédents : `envFilePath: ['.env', '.env.production']`. Les variables déjà définies dans l'environnement système ont la priorité."
  },
  {
    "questionText": "Que se passe-t-il si on utilise `ConfigModule.forRoot({ ignoreEnvFile: true })` ?",
    "options": [
      "Les variables ne sont accessibles que depuis `process.env`",
      "Le module ignore tous les fichiers `.env` et utilise uniquement les variables d'environnement réelles du système",
      "Le module lève une erreur si aucun fichier `.env` n'existe",
      "Les variables sont rechargées automatiquement à chaque requête"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Avec `ignoreEnvFile: true`, le `ConfigModule` ne charge aucun fichier `.env`. Il se base uniquement sur les variables d'environnement définies dans le système d'exploitation ou le conteneur. Utile en production."
  },
  {
    "questionText": "Quelle est la méthode pour ajouter une valeur par défaut lors de la récupération d'une variable avec `ConfigService` ?",
    "options": [
      "`this.configService.get('PORT', 3000)`",
      "`this.configService.get('PORT') || 3000`",
      "`this.configService.getOrDefault('PORT', 3000)`",
      "`this.configService.get('PORT', { default: 3000 })`"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Le second paramètre de `get()` est la valeur par défaut : `this.configService.get('PORT', 3000)`. Si `PORT` n'est pas défini, la méthode retourne 3000."
  }
]